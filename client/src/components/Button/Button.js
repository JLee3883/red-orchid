import React from 'react';



const Button = ({isSubmit, }) => {
    return (
        <div>
            <Button 
            onClick={onClick} type={type}>

            </Button>
           
        </div>

    );
};
export default Button;